package coffee;

import coffee.recipe.Recipe;

public class Outlet extends Thread{
    /**
     * lock total resources, decrement total resources by recipe.ingredients, remove lock, append to result list, return
     */

    void pour(Recipe recipe)
    {

    }
}
